Just edit the test.h with the value you want to test, then run "bash diff.sh", that will print u difference between real printf and your printf if there is a diff

(For each test you must write only what is inside the parenthesis)
